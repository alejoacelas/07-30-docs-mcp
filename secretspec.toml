[project]
name = "07-30-docs-mcp"
revision = "1.0"

# Extend configurations from subdirectories
# extends = [ "subdir1", "subdir2" ]

[profiles.default]
DOCS_MCP_TOKEN_KEY = { description = "32-byte base64url key for broker record encryption", required = true }
GOOGLE_OAUTH_CLIENT_ID = { description = "Google Web OAuth client ID", required = true }
GOOGLE_OAUTH_CLIENT_SECRET = { description = "Google Web OAuth client secret", required = true }
MCP_OAUTH_CLIENT_SECRET = { description = "Claude-to-broker OAuth client secret", required = true }
